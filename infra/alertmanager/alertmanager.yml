global:
  resolve_timeout: 5m

route:
  receiver: "default"
  group_by: ["alertname", "instance"]
  group_wait: 10s
  group_interval: 2m
  repeat_interval: 4h

receivers:
  - name: "default"
    webhook_configs:
      - url: "http://localhost:1/blackhole"

